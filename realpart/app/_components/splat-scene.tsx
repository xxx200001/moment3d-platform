// 重导出所有内容，保持向后兼容
export {
  SplatScene,
  parseBackendMetadata,
  DEFAULT_CAMERA_METADATA,
  isMobileDevice,
} from "./splat-viewer";

export type {
  CameraIntrinsics,
  CameraMetadata,
  BackendMetadata,
  ViewMode,
} from "./splat-viewer";
